# Figure provenance map

All five figures are generated by `build_artifacts.py` from the normalized `results.csv` and derived `summary.json` extracted from the formal run log.

| Figure | Evidence rows / fields | Transformation | Intended claim |
|---|---|---|---|
| `headline-result.png` | primary, with-LS costs paired with ASAP by instance | median heuristic ÷ ASAP ratio per variant | all heuristics reduce median carbon cost |
| `deadline-sensitivity.png` | primary `pressWR-LS`, `deadline_factor` | median paired ratio at each deadline factor | flexibility increases savings |
| `local-search-gap.png` | matched atacseq + bacass with/no-LS pairs | mean with-LS ÷ without-LS ratio | local-search magnitude differs from paper |
| `runtime-scaling.png` | runtime-profile with-LS rows | median and maximum algorithm time by task scale | runtime grows sharply with scale |
| `robustness-heatmap.png` | primary `pressWR-LS`, scenario and deadline | median paired ratio per cell | scenario sensitivity and S1 negative control |

Paper reference values are transcribed from arXiv:2507.08725v2. Observed values are computed directly by the script; no values are manually entered into the plotted series.
